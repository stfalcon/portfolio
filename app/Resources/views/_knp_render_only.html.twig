{% extends 'knp_menu.html.twig' %}

{% block item %}
    {% if item.attribute('render') is not empty %}
        {{ item.label|raw }}
    {% else %}
        {{ parent() }}
    {% endif %}
{% endblock %}